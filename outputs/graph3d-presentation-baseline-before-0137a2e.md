# Graph3D presentation baseline

Reference: `0137a2e`, read directly from the immutable Git object database.

New presentation diagnostic fields exposed before the redesign: 1 of 13.

The historical renderer exposed `cameraPreset`, but did not expose the new
presentation timing, painted-count, sidebar, or frame-quality diagnostics. This
is therefore a content-free structural baseline; it intentionally contains no
invented renderer timings or screenshots.
